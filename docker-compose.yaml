version: '3.9'

services:
  db:
    image: postgres
    restart: always
    ports:
      - 5432:5432
    environment:
      - POSTGRES_DB=ipi_jva320_web
      - POSTGRES_USER=ipi
      - POSTGRES_PASSWORD=ipi